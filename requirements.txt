# Pipeline de données (collect_day.py / interpret_day.py / soil_dynamic.py)
schedule
xarray
rioxarray
rasterio
geopandas>=1.1.2
pandas
numpy
scipy
h5py
netcdf4
requests>=2.33.0
urllib3>=2.7.0
idna>=3.15
matplotlib
Pillow>=12.2.0

# Modèle SDM probabiliste (scripts/train_sdm.py + scoring fruiting_live).
# Version ÉPINGLÉE sur 1.7.x : les modèles .pkl sont sensibles à la version de sklearn
# au (dé)picklage. Le serveur prod est en Python 3.10 → sklearn plafonne à 1.7.2
# (1.8+ exige Python ≥ 3.11). On entraîne donc les modèles en 1.7.x pour qu'ils
# chargent sans avertissement ni résultat faussé côté serveur.
scikit-learn>=1.7,<1.8

# Application web (server.py + champi_core.py + web/)
fastapi
uvicorn[standard]
itsdangerous
bcrypt
PyYAML
python-dotenv>=1.2.2
